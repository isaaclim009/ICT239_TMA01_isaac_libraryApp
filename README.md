# SG Library App Setup Guide

## 1. Clone the Repository
```bash
git clone <repository-url>
cd ICT239_TMA01_isaac_libraryApp

python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

flask --debug run
